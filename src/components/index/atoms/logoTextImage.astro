---
interface Props {
  src?: string;
  alt?: string;
  className?: string;
}

const {
  src = "/global/LOGO-BLANCO-TEXTO.webp",
  alt = "PrometeonDev - Software Development",
  className = "",
} = Astro.props as Props;
---

<img
  src={src}
  alt={alt}
  loading="lazy"
  class={`w-full md:max-w-[60%] xl:max-w-[50%] h-auto ${className}`}
/>
